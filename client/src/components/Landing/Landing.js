import React from 'react';
import './Landing.css';

const Landing = () => {
    return (
        <div className="App-landing">
            <h1>Weather App</h1>
            <h5>Compare the forecasts of multiple cities side-by-side.</h5>
            <p>Click the "+" button in the bottom right corner to add cities to compare.</p>
        </div>
    );
};

export default Landing;